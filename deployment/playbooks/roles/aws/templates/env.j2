DJANGO_DEBUG="False"
HOUNDIGRADE_AWS_AVAILABILITY_ZONE="{{ec2_asg_az}}"
HOUNDIGRADE_AWS_AUTOSCALING_GROUP_NAME="{{ec2_autoscaling_group.auto_scaling_group_name}}"
HOUNDIGRADE_ECS_CLUSTER_NAME="{{ecs_cluster.cluster.clusterName}}"

AWS_ACCESS_KEY_ID="{{iam_user.user_meta.access_keys[0].access_key_id}}"
AWS_SECRET_ACCESS_KEY="{{iam_user.user_meta.access_keys[0].secret_access_key}}"

AWS_SQS_ACCESS_KEY_ID="{{iam_user.user_meta.access_keys[0].access_key_id}}"
AWS_SQS_SECRET_ACCESS_KEY="{{iam_user.user_meta.access_keys[0].secret_access_key}}"

AWS_NAME_PREFIX="{{env}}-"

AWS_CLOUDTRAIL_EVENT_URL="{{ sqs_queue.queue_arn | regex_replace('^arn.+sqs.+us-east-1:(?P<account_id>\d+):(?P<sqs_queue_name>\S+)$', 'https://sqs.us-east-1.amazonaws.com/\\g<account_id>/\\g<sqs_queue_name>')}}"
AWS_S3_BUCKET_NAME="{{s3_bucket.name}}"

CLOUDIGRADE_ENVIRONMENT="{{env}}"
